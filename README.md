# Курсовая_работа_1

## Директории:

## src:


### **utils.py**
```python
def read_xlsx_file 
```
* функция для чтения XLSX файлов. 
Принимает строку - путь к файлу, возвращает список словарей с данными.

```python
def greeting 
```
* Функция отработки приветственного сообщения в зависимости от времени. 

```python
def cards 
```
* Функция обработки xlsx файла. Определяет последние 4 цифры номера карты.

```python
def top_transactions 
```
* Функция определения топ-5 транзакций по сумме платежа.

```python
def currency_rates 
```
* Функция для запроса курса валют у стороннего API.

```python
  def stock_prices
```
* Получает текущие цены акций.

### **reports.py**

```python
  def save_report
```
* Декоратор для функций отчетов, записывает результат в файл. Можно записывать с параметром названия файла или без.

```python
  def spending_by_category
```
* Возвращает траты по категории за последние 3 месяца от переданной даты.

### **logger_config.py**
**Модуль с настройками логгера проекта.**
```python
def setup_logger
``` 
* функция настроек логгера проекта. 
Импортируется в другие модули, используется для создания лог файлов в папке logs с информацией о результате работы функций.
Каждый раз перезаписывается, для экономии памяти.

### **tests.py**
**Директория содержащая тесты для модулей и функций проекта.**